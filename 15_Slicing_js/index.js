const fullname = "BroCode";
let firstname = fullname.slice(0,3);  // slice upto 0 to 2 char index's
let lastname = fullname.slice(3,7);   // slice upto 3 to 6 char index's
console.log(firstname);
console.log(lastname);